先执行：
cd ~/Desktop/code/medical-his-backend
启动前端：
mvn spring-boot:run


medical-his-backend/
├── pom.xml                                  # 【核心】Maven 依赖与版本配置文件
├── src/
│   └── main/
│       ├── java/com/hospital/medicalhisbackend/
│       │   ├── MedicalHisBackendApplication.java  # 【入口】Spring Boot 主启动类 (带 @MapperScan)
│       │   │
│       │   ├── controller/                  # 🟢 控制层：负责接收前端请求并返回 JSON 数据
│       │   │   └── DepartmentController.java 
│       │   │
│       │   ├── service/                     # 🟡 业务层：负责编写核心业务逻辑 (事务处理等)
│       │   │   ├── DepartmentService.java       (接口)
│       │   │   └── DepartmentServiceImpl.java   (实现类)
│       │   │
│       │   ├── mapper/                      # 🔵 数据层：继承 BaseMapper，直接拥有对表的增删改查能力
│       │   │   └── DepartmentMapper.java
│       │   │
│       │   └── entity/                      # 🟣 实体层：Java 对象，字段与 MySQL 表结构严格一一对应
│       │       └── Department.java
│       │
│       └── resources/
│           └── application.properties       # 【配置】全局配置：数据库 URL、账号密码、服务端口(8080)等


1. 基础运行环境 (Environment)

Java 版本：JDK 17 (必须是 17，适配最新的 Spring 架构)

构建工具：Maven 3.8+ (你本机目前是 3.9.14)

数据库：MySQL 8.0+

2. 核心框架依赖 (pom.xml)

Spring Boot 核心基座：3.2.4 (通过 <parent> 标签全局锁定)

    包含：spring-boot-starter-web (自带 Tomcat 10)

持久层框架 (ORM)：MyBatis-Plus 3.5.5

    坐标：mybatis-plus-spring-boot3-starter (注意必须要带 boot3 的后缀，否则不兼容新架构)

MySQL 驱动包：由 Spring Boot 自动管理版本 (通常是 8.x)

    坐标：mysql-connector-j

代码简化神器：Lombok (自动生成 Getter/Setter，由 Spring Boot 自动管理版本)